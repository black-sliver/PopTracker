-- This will be called in a different context. We don't have access to any of the globals.
-- Arguments can be passed as global arg

print(tostring(arg))

-- return provides the result value for the callback

return "something"
