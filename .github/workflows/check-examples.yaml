name: Check examples

on:
  push:
    paths:
      - '.github/workflows/check-examples.yaml'
      - 'schema/packs/**'
      - 'examples/**'
  pull_request:
    paths:
      - '.github/workflows/check-examples.yaml'
      - 'schema/packs/**'
      - 'examples/**'
  workflow_dispatch:

jobs:

  check-examples:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Check ap-storage-example
        uses: PopTracker/pack-checker-action@572a3854bdc8929b767d3f0454946c2e8eafac09
        with:
          schema: schema/packs/
          pack: examples/ap-storage-example
          strict: true
          check-legacy-compat: false

      - name: Check hosted_item
        uses: PopTracker/pack-checker-action@572a3854bdc8929b767d3f0454946c2e8eafac09
        with:
          schema: schema/packs/
          pack: examples/hosted_item
          strict: true

      - name: Check luminosity-test
        uses: PopTracker/pack-checker-action@572a3854bdc8929b767d3f0454946c2e8eafac09
        with:
          schema: schema/packs/
          pack: examples/luminosity-test
          strict: true

      - name: Check margin-test
        uses: PopTracker/pack-checker-action@572a3854bdc8929b767d3f0454946c2e8eafac09
        with:
          schema: schema/packs/
          pack: examples/margin-test
          strict: true

      - name: Check template_pack
        uses: PopTracker/pack-checker-action@572a3854bdc8929b767d3f0454946c2e8eafac09
        with:
          schema: schema/packs/
          pack: examples/template_pack
          strict: true

      - name: Check uat-example
        uses: PopTracker/pack-checker-action@572a3854bdc8929b767d3f0454946c2e8eafac09
        with:
          schema: schema/packs/
          pack: examples/uat-example
          strict: true
